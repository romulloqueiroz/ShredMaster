import { gradients } from '@styles'

export const COLOR_LIST = Object.keys(gradients)

// Full color list. Change for a fix value to limit the number of colors
export const SLICE = COLOR_LIST.length
// const SLICE = 15

