import * as Math from './Math'
import { 
  addOpacity, 
  getPx, 
  truncateString, 
  secondsToMinutes, 
  getModeColor, 
  getGradientColor,
  minutesToSeconds,
} from './utilities'
import SecureStore from './SecureStore'

export {
  Math,
  addOpacity,
  SecureStore,
  truncateString,
  getPx,
  secondsToMinutes,
  getModeColor,
  getGradientColor,
  minutesToSeconds,
}